# Data dirs (large, regenerated, not source)
books/
index/
modules/
answers/
output/
logs/
*.log

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
*.egg-info/
build/
dist/

# IDE / OS
.vscode/
.idea/
.DS_Store
*.swp
*.swo

# Test artifacts
medrack_test/
test_*.pdf
*_chunks/

# Local env
.env
venv/
.venv/

# State files (transient, regenerated by bot)
state/

# Runtime state (PID files, startup logs) — created by
# start_medrack.sh, not source. Committed scripts know to
# write here; we don't want to track it.
.medrack/

# Benchmark run outputs (large, regenerated, not source)
benchmarks/runs/

# Operator inbox (reference PDFs sent to the bot, not source)
inbox/
