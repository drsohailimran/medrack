"""medrack.module — per-module processing subpackage.

Currently exposes the text-extraction wrapper that the question/chapter
parsers depend on. Each task in Stage 2.3 (M1–M6) adds its own module
under this package.
"""
